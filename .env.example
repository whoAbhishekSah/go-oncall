# Environment variables for OnCall Scheduler

# Database Configuration
DATABASE_URL=postgres://oncall:oncall123@localhost:5432/oncall?sslmode=disable

# Slack Configuration
SLACK_TOKEN=xoxb-your-slack-bot-token-here
SLACK_CHANNEL=#oncall

# Docker Compose Database Configuration (used by migrate.sh)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=oncall
DB_USER=oncall
DB_PASSWORD=oncall123